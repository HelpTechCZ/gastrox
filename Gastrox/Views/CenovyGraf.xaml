<UserControl x:Class="Gastrox.Views.CenovyGraf"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <!-- Canvas se překresluje v code-behindu (Redraw) při SizeChanged a při změně Body. -->
    <Canvas x:Name="Plocha" ClipToBounds="True" Background="Transparent"/>
</UserControl>
