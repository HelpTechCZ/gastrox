<Window x:Class="Gastrox.Views.RenameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Přejmenovat"
        Height="160" Width="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F3F3F3"
        FontFamily="Segoe UI"
        FontSize="13">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Nový název:" Margin="0,0,0,8"/>
        <TextBox Grid.Row="1" x:Name="TxtNazev" VerticalAlignment="Top" Padding="8"
                 KeyDown="TxtNazev_KeyDown"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Zrušit"   Width="90" Margin="0,0,8,0" Click="Zrusit_Click"/>
            <Button Content="Uložit"   Width="90" IsDefault="True" Click="Ulozit_Click" Background="#0078D4" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>
