<Window x:Class="Gastrox.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:views="clr-namespace:Gastrox.Views"
        Title="Gastrox – Skladový software"
        Height="800" Width="1280"
        WindowStartupLocation="CenterScreen"
        Background="#F3F3F3"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        FontFamily="Segoe UI">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Boční menu -->
        <Border Grid.Column="0" Background="#202020" CornerRadius="0 16 16 0">
            <StackPanel Margin="16">
                <TextBlock Text="GASTROX" Foreground="White" FontSize="22" FontWeight="Bold" Margin="8,16,8,24"/>

                <Button Content="Dashboard"   Style="{StaticResource MaterialDesignFlatButton}" Foreground="White" HorizontalContentAlignment="Left" Margin="0,4" Click="Dashboard_Click"/>
                <Button Content="Sklad"       Style="{StaticResource MaterialDesignFlatButton}" Foreground="White" HorizontalContentAlignment="Left" Margin="0,4" Click="Sklad_Click"/>
                <Button Content="Příjemky"    Style="{StaticResource MaterialDesignFlatButton}" Foreground="White" HorizontalContentAlignment="Left" Margin="0,4" Click="Prijemky_Click"/>
                <Button Content="Výdejky"     Style="{StaticResource MaterialDesignFlatButton}" Foreground="White" HorizontalContentAlignment="Left" Margin="0,4" Click="Vydejky_Click"/>
                <Button Content="Inventury"   Style="{StaticResource MaterialDesignFlatButton}" Foreground="White" HorizontalContentAlignment="Left" Margin="0,4" Click="Inventury_Click"/>
                <Button Content="Reporty"     Style="{StaticResource MaterialDesignFlatButton}" Foreground="White" HorizontalContentAlignment="Left" Margin="0,4" Click="Reporty_Click"/>
            </StackPanel>
        </Border>

        <!-- Hlavní obsah -->
        <ContentControl x:Name="MainContent" Grid.Column="1" Margin="24"/>
    </Grid>
</Window>
